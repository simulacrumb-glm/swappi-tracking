{
  "name": "swappi-tracking",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "export NODE_OPTIONS; sentio test",
    "gen": "sentio gen",
    "build": "sentio build",
    "upload": "sentio upload",
    "postinstall": "sentio gen"
  },
  "dependencies": {
    "@sentio/sdk": "^2.20.1"
  },
  "devDependencies": {
    "@sentio/cli": "^2.9.2",
    "typescript": "^5.0.3"
  }
}